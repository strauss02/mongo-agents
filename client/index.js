const leftTab = document.querySelector(
  'body > main > div.top-row > span.tab.left-tab'
)

const centerTab = document.querySelector(
  'body > main > div.top-row > span.tab.center-tab'
)

const rightTab = document.querySelector(
  'body > main > div.top-row > span.tab.right-tab'
)

const newCityField = document.querySelector('.new-city-field')
const newCityField = document.querySelector('.city-field')
const newCityField = document.querySelector('.agent-field')
